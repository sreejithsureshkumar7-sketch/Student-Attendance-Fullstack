import {useState} from "react";import API from "../services/api.js";
export default function Login({setUser}){const [email,setEmail]=useState("admin@gmail.com");const [password,setPassword]=useState("admin123");const [msg,setMsg]=useState("");
const login=async(e)=>{e.preventDefault();setMsg("Checking...");try{const res=await API.post("/auth/login",{email,password});localStorage.setItem("token",res.data.token);localStorage.setItem("user",JSON.stringify(res.data.user));setUser(res.data.user)}catch(err){setMsg(err.response?.data?.message||"Login failed")}};
return <div className="loginPage"><form className="loginCard" onSubmit={login}><h1>Student Attendance Pro</h1><p>Final Full Stack Upgrade</p><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><input value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" type="password"/><button>Login</button><small>Default: admin@gmail.com / admin123</small><b className="msg">{msg}</b></form></div>}
